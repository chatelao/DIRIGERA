# Roadmap

- [ ] Create for every subdirectory of `specifications/markdown` a file in the subdirectory called `TEST_COVERAGE_<subdirectory>.md` with a table of the coverage for each file in the subdirectory.
